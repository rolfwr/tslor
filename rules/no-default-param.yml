id: no-default-param
language: TypeScript
severity: error
message: "Do not use function parameters default values to avoid updating call site to consider extended function capabilities and semantics. If function capabilities and semantics are genuinely optional with mainstream and sensible baseline behavior see options-object-pattern skill for alternatives."
rule:
  kind: required_parameter
  regex: '[^=>]= '
